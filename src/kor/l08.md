# Classes & Objects


- `class` 정의: `__init__`(생성자, modifier), `__str__`(print용, pure function)
- Pure function(입력을 변경하지 않고 새 값을 반환) vs. modifier(`self`의
  상태를 직접 변경)
- Getter/setter 패턴: `getX()`, `setX(v)`
- 객체를 매개변수/반환값으로 사용하기 (`Point`를 받아 새 `Point`를 반환하는
  `add(self, p)` 등)
